{
  "_instructions": "Legacy staff file. Day 8: email/password login has been retired and these entries are no longer used for sign-in. Authentication is Google-only and every user's roles live in the database. This list is intentionally empty (no passwords are stored in the repo). Do not add accounts here.",
  "users": []
}
